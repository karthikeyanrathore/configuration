# Turn off that annoying start up message
   startup_message off
    
# Turn the even more annoying whole-screen-flash-on-tab-complete "feature"
    vbell off
      
# I use control+a for beginning-of-line, this remaps it to the tick/tilda key
      escape ``

# Bind tick+e to show the tick character since tick+tick is too convenience to toggle windows
escape `e

# Window list at the bottom. hostname, centered tabs and redmarked active windows:
#shelltitle '$ |'
hardstatus alwayslastline
# https://superuser.com/questions/429493/gnu-screen-prepending-dollar-signs-to-window-titles
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-w%?%{r}(%{W}%n*%t%?(%u)%?%{r})%{w}%?%+w%?%? %= %{g}][%{Y} %m/%d %{W}%c %{g}]'

defscrollback 50000
